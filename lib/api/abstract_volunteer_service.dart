/* import 'firebase_volunteer_service.dart';

abstract class AbstractVolunteerService {
  // Stream for real-time UI updates (Direct Firestore Read)
  Stream<List<VolunteerOpportunity>> streamLocalOpportunities(
    String municipalityL1,
  );

  // Secure mutation that checks logic on server (Cloud Function Write)
  Future<void> checkInToOpportunity({
    required String opportunityId,
    required String memberId,
  });
} */
